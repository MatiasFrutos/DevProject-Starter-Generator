# __PROJECT_NAME__

Backend Express starter generated with DevProject Starter Generator.

## Overview

This is a clean Node.js and Express API starter.

It includes a modular backend structure with routes, controllers, services, repositories, configuration files, middlewares and utilities.

## Stack

```txt
Node.js
Express
CORS
Dotenv
Morgan
```

## Requirements

```txt
Node.js 18+
npm
```

## Install

```bash
npm install
```

## Run in development

```bash
npm run dev
```

## Run in production mode

```bash
npm start
```

## Default server

By default, the API runs on:

```txt
http://localhost:3000
```

## Available endpoints

### Base API route

```txt
GET /api
```

Example response:

```json
{
  "ok": true,
  "service": "__PROJECT_NAME__",
  "message": "Base API route"
}
```

### Healthcheck

```txt
GET /api/health
```

Example response:

```json
{
  "ok": true,
  "service": "__PROJECT_NAME__",
  "status": "online",
  "timestamp": "2026-05-13T22:00:00.000Z"
}
```

## Project structure

```txt
__PROJECT_NAME__/
  package.json
  README.md
  .env.example
  src/
    server.js
    app.js
    config/
      cors.js
      env.js
    controllers/
      health.controller.js
    middlewares/
      error.middleware.js
      not-found.middleware.js
    repositories/
      base.repository.js
    routes/
      index.routes.js
    services/
      health.service.js
    utils/
      http-error.js
      response.js
```

## Main files

### src/server.js

Server entry point.

It loads environment variables, creates the Express app and starts the HTTP server.

### src/app.js

Express application setup.

It registers:

```txt
CORS
JSON parser
URL encoded parser
Morgan logger
API routes
404 middleware
Error middleware
```

### src/config/env.js

Centralized environment configuration.

Default values:

```txt
PORT=3000
NODE_ENV=development
CORS_ORIGIN=*
```

### src/config/cors.js

CORS middleware configuration.

### src/routes/index.routes.js

Main API router.

Current routes:

```txt
GET /api
GET /api/health
```

### src/controllers/health.controller.js

Controller for base API and healthcheck responses.

### src/services/health.service.js

Service layer for healthcheck data.

### src/middlewares/not-found.middleware.js

Handles routes that do not exist.

Example response:

```json
{
  "ok": false,
  "error": "ROUTE_NOT_FOUND",
  "path": "/invalid-route"
}
```

### src/middlewares/error.middleware.js

Centralized error handler.

### src/repositories/base.repository.js

Base repository example.

It includes placeholder methods:

```txt
list
findById
create
update
remove
```

### src/utils/http-error.js

Reusable HTTP error class.

### src/utils/response.js

Response helpers for success and error responses.

## Environment variables

Create a .env file using .env.example as reference.

```env
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5500
```

## Scripts

```bash
npm run dev
```

Runs the API using Node watch mode.

```bash
npm start
```

Runs the API normally.

## Development notes

This starter is designed to be simple, readable and easy to extend.

It works well as a base for:

```txt
REST APIs
Internal tools
Admin panels
ERP systems
CRM systems
Dashboards
Backend services
```

## Suggested next steps

```txt
Add database connection
Add authentication
Add JWT middleware
Add validation layer
Add module-based routes
Add logging service
Add request audit trail
Add PostgreSQL repository
Add Docker support
```

## Generated by

DevProject Starter Generator.